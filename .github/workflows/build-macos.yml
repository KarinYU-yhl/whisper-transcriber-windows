name: Build macOS installer

# Builds the macOS .app and packages it into a drag-to-Applications .dmg on a
# GitHub-hosted macOS runner. Run it manually (Actions tab -> Run workflow) or
# push a tag like "mac-v1.0.27" to also publish a GitHub Release with the .dmg.
on:
  workflow_dispatch:
  push:
    tags:
      - "mac-v*"

permissions:
  contents: write

jobs:
  build:
    # macos-14 is Apple Silicon (arm64). These runners are far more available
    # than the Intel (macos-13) ones, so builds start almost immediately. The
    # resulting app targets Apple Silicon Macs (M1/M2/M3/M4). For Intel Macs,
    # switch this to macos-13 (Intel runners can queue for a long time).
    runs-on: macos-14

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: macos
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build .app and .dmg
        working-directory: macos
        run: python build.py

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: WhisperTranscriber-macOS
          path: macos/dist/*.dmg
          if-no-files-found: error

      - name: Publish release (tag pushes only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: macos/dist/*.dmg
          generate_release_notes: true
